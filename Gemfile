source 'https://rubygems.org'

gem 'bundler'
gem 'rake'
gem 'hanami',       '0.7.3'

gem 'mongoid'
gem 'mongoid-slug' # generates slugs for models

gem 'bcrypt'

group :development do
  gem 'byebug'
end

group :test do
  gem 'minitest'
  gem 'capybara'
end

group :production do
  # gem 'puma'
end
